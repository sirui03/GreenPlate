<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidGreenPlate"
        tools:targetApi="31">
        <activity
            android:name=".views.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".views.HomeActivity" />
        <activity android:name=".views.IngredientsActivity" />
        <activity android:name=".views.InputMealActivity" />
        <activity android:name=".views.RecipeActivity" />
        <activity android:name=".views.ShoppingListActivity" />
        <activity android:name=".views.CreateAccountActivity" />
        <activity android:name=".views.LoginActivity"/>
        <activity android:name=".views.UserInfoActivity"/>
    </application>

</manifest>